name: king_cache
description: "Flutter package for caching api data and markdown content to disk and improving app performance. It uses the http package to make api calls and use cache directory to cache data"
version: 0.0.60
homepage: "https://github.com/king-technologies/king_cache"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  device_info_plus: ^13.2.0
  firebase_analytics: ^12.4.6
  firebase_crashlytics: ^5.2.7
  flutter:
    sdk: flutter
  http: ^1.6.0
  in_app_update: ^5.0.0
  intl: ^0.20.3
  local_auth: ^3.0.2
  logger: ^2.7.0
  package_info_plus: ^10.2.1
  path_provider: ^2.1.6
  permission_handler: ^13.0.0
  url_launcher: ^6.3.2
  web: ^1.1.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

platforms:
  android:
  ios:
  web:
  windows:
  macos:
  linux:
